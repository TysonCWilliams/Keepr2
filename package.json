{
  "name": "wwwsrc",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm run setup:client && npm run setup:server",
    "setup:client": "cd wwwsrc.client && npm i",
    "setup:server": "cd wwwsrc.server && npm i"
  }
}
